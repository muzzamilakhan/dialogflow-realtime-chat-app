# Backend Notes

This Laravel app powers auth, chat persistence, and Dialogflow ES REST integration for the realtime chat project.

Use the project root README for full setup instructions:

- [Project README](/C:/wamp64/www/dialogflow-realtime-chat-app/README.md)

Important backend commands:

```bash
composer install
php artisan migrate --seed
php artisan test
```
